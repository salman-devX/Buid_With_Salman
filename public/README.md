# /public assets to add

These are referenced by the site but not included (no fake/placeholder
binaries are shipped):

- `favicon.ico` — your site icon
- `og-image.png` — 1200x630 image used for social share previews
- `resume.pdf` — your resume, linked from the "Download Resume" button
- `projects/` — optional folder for project screenshots referenced in
  `src/data/projects.ts` via the `image` field
